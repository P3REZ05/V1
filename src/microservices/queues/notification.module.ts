import { Module } from '@nestjs/common';

@Module({
  providers: [], // Agrega los servicios que necesites aquí
  exports: [], // Exporta lo que otros módulos deben usar
})
export class NotificationModule {}
